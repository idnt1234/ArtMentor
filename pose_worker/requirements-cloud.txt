# The installer provides torch==2.1.* and torchvision==0.16.* in an isolated
# Python 3.10 environment; the Docker base image provides the same pair.
# MMCV's wheel is selected for CUDA 12.1 / PyTorch 2.1 / Python 3.10.
--find-links https://download.openmmlab.com/mmcv/dist/cu121/torch2.1/index.html

numpy==1.26.4
opencv-python==4.10.0.84
mmcv==2.1.0
mmengine==0.10.7
mmdet==3.2.0
# MMPose is installed separately with --no-deps. Its 1.3.2 package metadata
# still pulls the obsolete Chumpy package, which is unused by our 2D RTMPose
# inference path and fails modern isolated builds.
json-tricks==3.17.3
matplotlib==3.10.0
munkres==1.1.4
pillow==11.1.0
scipy==1.15.1
xtcocotools==1.14.3
fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
